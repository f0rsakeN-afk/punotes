export interface AssignMentTypes {
  title: string;
  subject: string;
  url: string;
  id: number;
}

export interface PYQTypes {
  id: number;
  title: string;
  url: string;
}

export interface TeamMember {
  name: string;
  role: string;
  image: string;
  linkedin: string;
}
